version: '3.8'

services:
  jarvis-bot:
    build: .
    container_name: jarvis_bot
    restart: unless-stopped 
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials.json:/app/credentials.json:ro